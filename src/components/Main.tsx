import type { FC } from "react";
import "./Main.css"

export const Main
: FC = () => {
    return (
        <main>
            <div className="container">
                
            </div>
        </main>
    );
}
